#!groovy
pipeline {
    agent {
        label 'stable && docker && linux && ansible'
    }
    stages {
        stage('Build Docker Images') {
            steps {
		unstash 'artifacts'
		sh 'ls -la'
		sh 'ls -la target'
                sh 'ansible-playbook aurea-central-jervis/ansible/jervis.yml -i inventories/staging --tags docker-image-build '
            }
        }
        stage('Docker Service Deploy') {
            steps {
		unstash 'artifacts'
		sh 'ls -la'
		sh 'ls -la target'
                sh 'ansible-playbook aurea-central-jervis/ansible/jervis.yml -i inventories/staging --tags docker-service-deploy '
            }
        }
    }
}
